#!/bin/bash
# ubuntu (om in bash te coderen; wsl of bash kan ook)
# exit (om te stoppen; logout kan ook)

# ctrl-c (kopieert geselecteerde tekst en beëindigt een lopend proces)
# ctrl-v (plakt geselecteerde tekst)
# clear (maakt terminalscherm schoon)

# absoluut pad: refereert vanuit de root directory naar de locatie van een file
# of directory
# relatief pad: refereert vanuit de huidige directory naar de locatie van een
# file of directory

# pwd (toont huidige directory)
# realpath (toont absolute pad van gespecificeerde file of directory)
# which (toont absolute pad van gespecificeerde commando)

# ls (toont inhoud van directory; bestandsnamen)
# ll (toont inhoud van directory; permissions, hardlinks, owners, groups,
# bytes, timestamps en bestandsnamen)
# cat (toont inhoud van file; begin tot eind)
# tac (toont inhoud van file; eind tot begin)

# cd (verandert van directory)
# cd / (ga naar root)
# cd ~ (ga naar home)
# cd - (ga terug naar vorige directory)
# cd ../ (ga 1 stap terug)
# cd x/ (ga 1 stap verder; waarbij "x" de gewenste directory is)

# mkdir (maakt een directory)
# rmdir (verwijdert een directory)

# touch (maakt een file aan als die niet bestaat)
# touch -t JJJJ/MM/DD/hh/mm/ss (verandert timestamp van een file)

# cp (verplaatst en kopieert file of directory)
# mv (verplaatst en vernoemt file of directory)
# rm (verwijdert een file of lege directory)

# file (toont de extensie van een file)
# unzip (extraheert content van een zipfile)

# help (toont informatie over een ingebouwde commando)
# man (toont gebruiksaanwijzing van een commando)
# '/': (vul een zoekterm in)
# pijltoetsen: (voor navigatie)
# 'q': om te stoppen

# history (toont alle commando's die ooit gebruikt zijn)
# whoami (toont username)
# echo (toont opgegeven tekst)
# cal (toont huidige maand)
