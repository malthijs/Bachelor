#!/bin/bash
# awk '{print $0}' toont alle inhoud van een file
# awk '{print $NF}' toont de laatste kolom van een file

# awk '{if (...) {...} else (...) {...}}'
# awk 'BEGIN{...} {...} END{...}'
# awk -F (hiermee kan gespecificeerd worden hoe kolommen gescheiden worden)
# substr (string, start, length)
# length (string)
